language: objective-c
rvm: 1.9.3
before_script:
  - cd src/OCJiraFeedback
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet  
  - pod install
script:
  - xctool ONLY_ACTIVE_ARCH=NO
  - xctool test ONLY_ACTIVE_ARCH=NO
